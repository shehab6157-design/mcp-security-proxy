# Copy this file to .env (gitignored) and fill in real values.
# Get a bot token from @BotFather on Telegram, and your numeric chat_id by
# messaging the bot once and checking https://api.telegram.org/bot<token>/getUpdates
#
# trigger/run.py works without these set - notifications just print to
# stdout instead of reaching Telegram.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Required only for `red/orchestrator.py --executor caldera` (red/executor.py's
# CalderaExecutor) - a live MITRE Caldera server's API key, found in Caldera's
# own config or "Show API Key" in its UI.
CALDERA_API_KEY=
# Optional: defaults to http://localhost:8888 if unset.
CALDERA_URL=
